const auth = require('./auth');
const organization = require('./organization');
const datasource = require('./datasource');
const device = require('./device');

module.exports = { auth, organization, datasource, device };
